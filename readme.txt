Running it terminal:

To to run it via the source code use the following command
mvn clean spring-boot:run

or

to run it on a jar file, locate the jar file (target)
java -jar target/exam-0.0.1-SNAPSHOT.jar

log is in logs folder